/**
 * Core data key error is a list of errors in the data context.
 */
export const dataErrorKeys = {
    dataMustBeObject: 'core.error.data_must_be_object',
    dataIsNull: 'core.error.data_is_null'
} as const;